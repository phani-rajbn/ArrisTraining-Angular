import { BigTextDirective } from './big-text.directive';

describe('BigTextDirective', () => {
  it('should create an instance', () => {
    const directive = new BigTextDirective();
    expect(directive).toBeTruthy();
  });
});
